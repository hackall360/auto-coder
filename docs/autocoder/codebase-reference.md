# Auto-Coder Codebase Reference

_This file is auto-generated by `scripts/generate_codebase_reference.py`. Run the
script after modifying any modules to keep the inventory current._

## Top-level modules

### `TUI.py`
Textual-based terminal UI for interacting with Auto-Coder.
#### Public classes
- **ManagerStatusMessage**
    Message emitted when the manager publishes a status update.
- **ManagerResultMessage**
    Message emitted after the manager finishes handling a prompt.
- **PromptSubmitted**
    Message emitted when the user submits input through the prompt widget.
- **TranscriptWidget**
    Stream conversation entries between the user and Auto-Coder.
    - Methods:
        - `add_user_message()`
        - `add_manager_message()`
        - `add_system_message()`
- **PlanTrackerWidget**
    Render a simple table outlining the active execution plan.
    - Methods:
        - `reset_from_plan()`
        - `handle_status()`
- **BudgetMeterWidget**
    Display the budget consumption for each task.
    - Methods:
        - `handle_status()`
        - `apply_result()`
- **StatusFeedWidget**
    Live feed of status updates from the manager.
    - Methods:
        - `add_update()`
        - `add_message()`
- **PromptInput**
    Input widget dedicated to capturing user prompts.
    - Methods:
        - `watch_is_busy()`
        - `on_input_submitted()`
- **AutoCoderApp**
    Textual application hosting the Auto-Coder conversational interface.
    - Methods:
        - `compose()`
        - `action_cancel_request()`
        - `on_prompt_submitted()`
        - `on_manager_status_message()`
        - `on_manager_result_message()`
#### Top-level functions
- `run_tui()`

### `chat.py`
Utilities for interacting with LM Studio chat models.

This module provides convenience wrappers around the `lmstudio` Python SDK,
loosely following the quick-start snippets from
``docs/LMStudio/developer/python/llm-prediction/chat-completion.md``.
#### Public classes
- **ResponseStream**
    Iterator wrapper for streamed responses.

    Instances yield the fragments produced by :meth:`model.respond_stream` while
    exposing :meth:`result` and :meth:`wait_for_result` helpers analogous to the
    SDK's streaming example in the LM Studio documentation.
    - Methods:
        - `result()`
        - `wait_for_result()`
        - `text()`
- **ChatSession**
    Utility for managing a multi-turn conversation with LM Studio models.

    The helper mirrors the "Multi-turn Chat" walkthrough by reusing an
    :class:`lmstudio.Chat` instance between turns while offering convenience
    methods for sending user messages and streaming responses.
    - Methods:
        - `create()`
        - `add_user_message()`
        - `add_assistant_message()`
        - `append_user_input()` – Alias for :meth:`add_user_message` for parity with agent helpers.
        - `append_tool_response()` – Append a tool response message to the chat history.
        - `send()`
        - `send_stream()`
        - `set_tools()` – Replace the active tool list for subsequent :meth:`act` calls.
        - `act()`
#### Top-level functions
- `get_model()` – Return an LM Studio model handle using the high-level ``lms.llm`` helper.
- `respond()` – Generate a single response using :meth:`model.respond`.
- `act()` – Execute :meth:`model.act` with resolved tool definitions.
- `respond_stream()` – Stream a response in the style of the documentation's streaming samples.

### `core.py`
Core runtime orchestration utilities for Auto-Coder.
#### Public classes
- **PathSettings**
    Filesystem locations used by the runtime.

    Attributes
    ----------
    repo_root:
        Absolute path to the repository that should be inspected by context-aware
        agents. Defaults to the current working directory when not configured.
    workspace_root:
        Optional directory for ephemeral artifacts (for example, generated
        documentation or cached command outputs). When omitted the repository
        root is reused.
    artifact_root:
        Directory used by helper agents such as :class:`RunnerAgent` for
        persisted artifacts. Defaults to ``workspace_root / ".autocoder"`` when
        neither configured nor provided via environment variables.
- **ModelSettings**
    Model selection and agent-wide behavioural flags.
- **ResearchSettings**
    Options controlling the behaviour of the :class:`ResearchAgent`.
- **RepoContextSettings**
    Filters applied when building the repository semantic index.
- **AgentToggleSettings**
    Enable/disable specialist agents without code changes.
- **MemorySettings**
    Overrides controlling memory configuration resolution.
- **MCPSettings**
    Configuration for MCP server discovery and lifecycle management.
- **ManagerSettings**
    Controls for the top-level manager agent.
- **AutoCoderConfig**
    Aggregate configuration consumed by :class:`AutoCoderCore`.
- **AutoCoderCore**
    Central orchestrator responsible for wiring high-level agents together.
    - Methods:
        - `build_manager()`
        - `shutdown()`
#### Top-level functions
- `load_core_configuration()` – Load :class:`AutoCoderConfig` by merging config file, env, and overrides.

### `logging.py`
Compatibility shim that re-exports Python's standard :mod:`logging` package.

### `logging_config.py`
Centralized logging configuration utilities for Auto-Coder entrypoints.
#### Top-level functions
- `configure_logging()` – Install the Auto-Coder logging configuration.

### `main.py`
Command-line entry point bootstrapping the manager agent.
#### Top-level functions
- `main()` – Launch the Textual TUI entrypoint.

### `mcp_tooling.py`
Helpers for configuring and launching MCP servers.

This module defines a small collection of dataclasses that capture
configuration for different flavours of MCP servers.  A registry class
is provided to validate and normalise raw configuration payloads loaded
from ``config.json``.  For command based servers a small lifecycle helper
is included to manage subprocess creation, readiness detection and
shutdown behaviour.
#### Public classes
- **MCPConfigurationError**
    Raised when a configuration entry is invalid.
- **MCPServerConfig**
    Base dataclass for MCP server configuration entries.
    - Methods:
        - `kind()`
        - `descriptor()`
- **LocalMCPServerConfig**
    Configuration for locally running MCP servers.
    - Methods:
        - `kind()`
        - `descriptor()`
- **RemoteMCPServerConfig**
    Configuration for remote MCP servers accessible over the network.
    - Methods:
        - `kind()`
        - `descriptor()`
- **CommandMCPServerConfig**
    Configuration for MCP servers launched via a local command.
    - Methods:
        - `kind()`
        - `descriptor()`
- **CommandServerLifecycle**
    Lifecycle helper for command-based MCP servers.
    - Methods:
        - `start()`
        - `pid()`
        - `stdout_log()`
        - `stderr_log()`
        - `shutdown()`
- **MCPServerSpec**
    Normalized representation produced by :class:`MCPServerRegistry`.
- **MCPServerRegistry**
    Validate and normalise MCP server configuration entries.
    - Methods:
        - `from_loaded_config()`
        - `update()`
        - `get()`
        - `all()`
        - `build_specs()`
        - `start_all()`
        - `labels()`
        - `get_spec()`
        - `specs()`
        - `get_lifecycle()`
        - `shutdown_all()`
#### Top-level functions
- `load_mcp_config()` – Load the ``mcp_servers`` configuration section from ``config.json``.
- `register_mcp_servers()`

### `memory.py`
Utilities for configuring and working with agent memory stores.

This module defines a small abstraction layer that allows the rest of the
application to request different flavours of memory (for example, "short" or
"long" term storage) without coupling callers to a particular backend.  It also
includes helpers for loading configuration from environment variables or an
optional ``config.json`` file so that operators can adjust memory behaviour
without modifying code.
#### Public classes
- **MemoryMetadata**
    Metadata describing a memory record.

    Attributes
    ----------
    source:
        A free-form description of the origin of the memory.
    created_at / updated_at:
        Timestamps in UTC that track the lifecycle of the record.
    ttl_seconds:
        Optional time-to-live. ``None`` indicates that the record should not
        expire automatically.
    tags:
        Arbitrary labels that can be used for downstream filtering.
    importance:
        Optional importance score that downstream ranking systems may use.
    embedding_model:
        Identifier for the embedding model that produced the embedding vector
        stored in :class:`MemoryRecord`.
    attributes:
        Additional key/value metadata for downstream filtering.
    - Methods:
        - `touch()` – Update the ``updated_at`` timestamp to the current UTC time.
        - `is_expired()` – Return ``True`` if the TTL has elapsed.
        - `matches()` – Check whether the metadata satisfies the provided filters.
- **MemoryRecord**
    A single memory entry stored in a :class:`MemoryStore`.
    - Methods:
        - `with_score()` – Return a shallow copy of the record with an updated score.
- **MemoryQuery**
    Parameters describing how to fetch memories from a store.
- **MemoryStore**
    Abstract base class for memory store implementations.
    - Methods:
        - `add()` – Add a record to the store and return the stored value.
        - `update()` – Update an existing record and return the new value.
        - `delete()` – Remove a record from the store.
        - `get()` – Return a record by identifier.
        - `fetch()` – Return records that match the query.
        - `compact()` – Run maintenance tasks such as removing expired records.
- **InMemoryMemoryStore**
    Simple in-memory store used for defaults and testing.
    - Methods:
        - `add()`
        - `update()`
        - `delete()`
        - `fetch()`
        - `compact()`
        - `get()`
- **ShortTermMemoryStore**
    Redis-backed short term memory store leveraging hashes and sorted sets.
    - Methods:
        - `add()`
        - `update()`
        - `delete()`
        - `get()`
        - `fetch()`
        - `compact()`
        - `soft_delete()`
        - `refresh_ttl()`
        - `promote_to_long_term()`
        - `copy_to_long_term()`
        - `list_entries()`
        - `list_sessions()`
- **RedisMemoryStore**
    Backward compatible alias for the Redis-backed store.
- **LongTermMemoryStore**
    PostgreSQL-backed implementation designed for durable storage.
    - Methods:
        - `add()`
        - `update()`
        - `delete()`
        - `get()`
        - `record_audit_event()`
        - `fetch()`
        - `list_sessions()`
        - `compact()`
- **PostgresMemoryStore**
    Backward compatible alias for :class:`LongTermMemoryStore`.
- **CompositeMemoryStore**
    A store that fans operations out to multiple underlying stores.
    - Methods:
        - `add()`
        - `update()`
        - `delete()`
        - `get()`
        - `fetch()`
        - `compact()`
- **RedisSettings**
- **PostgresSettings**
- **StoreConfig**
    - Methods:
        - `copy()`
- **MemoryConfiguration**
    - Methods:
        - `for_scope()`
- **MemoryRouter**
    Resolve logical memory scopes into configured store instances.
    - Methods:
        - `get_store()`
        - `short_term()`
        - `long_term()`
        - `combined()`
        - `register()`
        - `scopes()`
        - `promote_to_long_term()`
        - `copy_to_long_term()`
        - `compact_with_summarizer()`
- **MemoryFacade**
    High-level helper exposing convenience operations across memory scopes.
    - Methods:
        - `search()` – Return records matching ``text`` from the requested ``scope``.
        - `list_sessions()`
        - `iter_session_messages()` – Return ordered conversation records for ``session_id``.
        - `add()` – Persist ``content`` into ``scope`` returning the stored record.
        - `update()` – Update an existing record and return the stored value.
        - `promote()` – Promote a short-term record into long-term storage.
        - `to_dict()` – Return a JSON-serialisable representation of ``record``.
- **ConversationMemoryHooks**
    Lifecycle hooks that persist conversation turns via :class:`MemoryFacade`.
    - Methods:
        - `on_round_start()`
        - `on_round_end()`
#### Top-level functions
- `register_embedding_provider()` – Register a custom embedding provider used during memory ingestion.
- `clear_embedding_providers()` – Clear all cached embedding providers.
- `resolve_embedding_provider()` – Return a cached embedding provider and associated model name.
- `load_config_json()` – Load ``config.json`` if present.
- `load_memory_configuration()` – Build a :class:`MemoryConfiguration` by merging config file and env vars.
- `build_memory_store()` – Instantiate a concrete :class:`MemoryStore` from configuration.
- `build_memory_router()` – Build a :class:`MemoryRouter` with stores configured for each scope.
- `get_shared_memory_router()` – Return the shared :class:`MemoryRouter`, constructing one on demand.
- `set_shared_memory_router()` – Override the shared router used by tooling and agents.
- `get_shared_memory_facade()` – Return a singleton :class:`MemoryFacade` backed by the shared router.
- `set_shared_memory_facade()` – Replace the globally shared :class:`MemoryFacade`.

### `session.py`
High-level session orchestration for LM Studio agent workflows.
#### Public classes
- **AgentRound**
    Container for per-round metadata captured by :class:`AgentSession`.
    - Methods:
        - `to_dict()`
- **AgentSession**
    Wrap LM Studio ``model.act`` calls with convenient state tracking.
    - Methods:
        - `model()`
        - `tools()`
        - `system_prompt()`
        - `transcript()`
        - `set_tools()`
        - `replace_mcp_tools()` – Update the active MCP tool definitions while preserving other tools.
        - `append_user_input()`
        - `append_tool_response()`
        - `act()`
        - `add_round_hooks()` – Append round lifecycle hooks without overriding existing callbacks.
        - `last_round()`
        - `tool_history()`

### `tooling.py`
_No module-level docstring._
#### Public classes
- **ToolSpec**
    Normalized representation of a tool definition.
    - Methods:
        - `to_payload()` – Return the structure expected by :meth:`model.act`.
- **ToolRegistry**
    Registry for collecting and validating tool definitions.
    - Methods:
        - `register()` – Register ``tool`` ensuring it is safe for exposure to the model.
        - `register_mcp_tool()`
        - `unregister()`
        - `get()`
        - `all()`
        - `resolve()` – Resolve tool references into normalized :class:`ToolSpec` instances.
#### Top-level functions
- `get_all_tools()` – Return a shallow copy of every registered tool specification.
- `get_tool()` – Return the normalized tool specification by ``name``.
- `get_tools()` – Return registered tools matching ``names`` (or all when ``None``).
- `resolve_tools()` – Resolve explicit tool definitions and names into normalized specifications.
- `register_tool()` – Register ``tool`` into the default registry.
- `register_mcp_tool()` – Register an MCP tool definition into the default registry.
- `unregister_tool()` – Remove ``name`` from the default registry if present.

## `agents/`

### `agents/__init__.py`
Convenience helpers for constructing :class:`AgentSession` instances.
#### Public classes
- **AgentBuilder**
    Fluent builder for assembling agent sessions.
    - Methods:
        - `with_toolsets()`
        - `with_tools()`
        - `with_tool_names()`
        - `with_mcp_servers()` – Schedule MCP server configurations to be registered at build time.
        - `with_model()`
        - `using_registry()`
        - `build()`
#### Top-level functions
- `clear_default_toolsets()` – Remove all globally registered default toolsets.
- `list_default_toolsets()` – Return the names of configured default toolsets.
- `register_default_toolset()` – Register a reusable toolset that can be referenced by ``create_agent``.
- `create_agent()` – Instantiate an :class:`AgentSession` configured with helper defaults.

### `agents/coder.py`
Autonomous coding agent that collaborates with the manager workflow.

The :class:`CoderAgent` is responsible for consuming implementation tasks
dispatched by :class:`~agents.manager.ManagerAgent`, consulting repository
context payloads, requesting additional guidance from LM Studio, and finally
applying changes to the working tree.  File mutations are performed via the
`internal.tools.file` and `internal.tools.patch` suites to ensure consistent
behaviour with other automation helpers.  Every applied diff is tracked so the
manager can surface reviewable artifacts or feed follow-up agents.
#### Public classes
- **MinimalPatch**
    Container describing a unified diff patch returned by the LLM.
    - Methods:
        - `to_dict()`
- **AppliedDiff**
    Record of an applied diff along with summary statistics.
    - Methods:
        - `to_dict()`
        - `changed_paths()`
- **ChangeSummary**
    Structured summary describing a change for the manager.
    - Methods:
        - `to_dict()`
- **CoderResult**
    Return payload produced by :class:`CoderAgent.run_task`.
    - Methods:
        - `to_dict()`
- **CoderAgent**
    Implementation-focused agent that produces reviewable code changes.
    - Methods:
        - `applied_diffs()`
        - `last_result()`
        - `run_task()`

### `agents/db_migration.py`
Database migration orchestration utilities and agent implementation.
#### Public classes
- **MigrationRecord**
    Representation of a migration file tracked inside a framework.
    - Methods:
        - `to_dict()`
- **SchemaMigrationPlan**
    Plan describing how to generate migrations for a framework.
    - Methods:
        - `to_dict()`
- **EphemeralDatabaseSpec**
    Lifecycle hooks for provisioning an ephemeral database runtime.
    - Methods:
        - `to_dict()`
- **MigrationResult**
    Outcome of running a migration generation workflow.
    - Methods:
        - `to_dict()`
- **DBMigrationAgent**
    Orchestrates schema migration generation across multiple frameworks.
    - Methods:
        - `plan_migration()` – Construct a migration plan for the requested framework.
        - `run_migration()` – Execute a migration plan and capture the resulting artefacts.
        - `register_ephemeral_database()` – Register an ephemeral database specification for later reuse.
        - `get_ephemeral_specs()`
        - `format_result()` – Create a concise textual summary for a migration result.

### `agents/dependency.py`
Dependency resolution helpers leveraging :class:`RunnerAgent`.
#### Public classes
- **LockfileDiffSummary**
    Summary of dependency changes extracted from a lockfile diff.
    - Methods:
        - `has_changes()`
        - `describe()`
        - `to_dict()`
        - `from_contents()`
        - `from_unified_diff()`
- **DependencyCacheDirective**
    Hint to persist package-manager specific caches between runs.
    - Methods:
        - `to_dict()`
        - `hint()`
- **DependencyResolution**
    Outcome of executing a dependency management command.
    - Methods:
        - `ok()`
        - `to_dict()`
        - `describe()`
- **DependencyBuildAgent**
    Utility agent orchestrating dependency related build actions.
    - Methods:
        - `run_task()`
        - `format_resolution()`

### `agents/doc.py`
Documentation drafting helpers for README, changelog, and walkthrough updates.
#### Public classes
- **DocumentationSummary**
    Aggregate overview constructed from relevant repository modules.
    - Methods:
        - `to_dict()`
- **ReadmeDraft**
    README snippet drafted by :class:`DocAgent`.
    - Methods:
        - `to_dict()`
- **ChangelogDraft**
    Structured changelog entry generated for a release.
    - Methods:
        - `to_dict()`
- **WalkthroughSection**
    Walkthrough segment describing an important module or workflow.
    - Methods:
        - `to_dict()`
- **DocumentationResult**
    Container bundling the full documentation draft output.
    - Methods:
        - `to_dict()`
- **DocAgent**
    Generate documentation updates grounded in repository state.
    - Methods:
        - `attach_research_agent()` – Attach or detach the research agent used for evidence gathering.
        - `draft_updates()` – Draft documentation snippets derived from repository context.
        - `format_result()` – Render a human-readable summary of the documentation drafts.
        - `to_structured_response()` – Convert the draft into a :class:`StructuredResponse`.

### `agents/eval.py`
Evaluation helpers for prompt regressions and structured summaries.
#### Public classes
- **PromptEvalResult**
    Outcome returned after issuing a single prompt to an :class:`AgentSession`.
    - Methods:
        - `to_dict()`
- **PromptComparison**
    Container capturing paired baseline and candidate prompt executions.
    - Methods:
        - `to_dict()`
- **RegressionSummary**
    Summary of all prompt comparisons produced by :class:`EvalAgent`.
    - Methods:
        - `to_dict()`
        - `failed()`
        - `passed()`
        - `total()`
        - `is_blocking()`
- **EvalAgent**
    Run paired prompt comparisons and compute regression summaries.
    - Methods:
        - `run()` – Execute an evaluation spec and return the aggregated summary.
        - `format_summary()` – Return a readable textual overview of the evaluation results.
        - `to_structured_response()` – Convert a regression summary into a :class:`StructuredResponse`.

### `agents/integrations.py`
Agents for working with CI/CD integrations, container builds, and releases.
#### Public classes
- **CIJobPlan**
    Declarative description of a CI pipeline job to be rendered.
    - Methods:
        - `to_dict()`
        - `with_rendered()` – Return a copy of the plan with ``rendered`` populated.
- **PipelineUpdateResult**
    Summary of a pipeline write operation.
    - Methods:
        - `to_dict()`
- **ReleaseMetadata**
    Structured representation of a release announcement.
    - Methods:
        - `to_dict()`
- **IntegrationsAgent**
    High-level helper for CI detection, pipeline authoring, and releases.
    - Methods:
        - `detect_pipelines()` – Detect existing CI systems using the repository context agent.
        - `render_pipeline()` – Render a pipeline template with the provided variables.
        - `apply_pipeline()` – Persist a rendered pipeline to disk.
        - `run_container_build()` – Execute a container build and return the resulting run report.
        - `prepare_release_metadata()` – Prepare a standardized release metadata payload.
        - `orchestrate_pipeline()` – Render and optionally apply a pipeline plan.

### `agents/manager.py`
High-level manager agent orchestrating DAG-driven task workflows.
#### Public classes
- **TaskBudget**
    Track quota consumption for an individual task.
    - Methods:
        - `consume()`
        - `remaining()`
        - `progress()`
        - `as_dict()`
        - `copy()`
- **ManagerStatusUpdate**
    Message surfaced to users describing manager workflow progress.
    - Methods:
        - `as_dict()`
- **ManagerResult**
    Aggregate output returned after the manager completes a workflow.
    - Methods:
        - `to_dict()`
- **ManagerAgent**
    Top-level orchestrator for user-facing conversations.
    - Methods:
        - `run()` – Execute the manager workflow for a new user input.
        - `attach_repo_context()` – Attach or replace the repo context agent used for focused queries.
        - `attach_test_critic()` – Attach or detach the fast-test critic responsible for gating completion.
        - `attach_eval_agent()` – Attach or detach the evaluation agent used for regression checks.
        - `attach_dependency_agent()` – Attach or detach the dependency build helper.
        - `request_focused_files()` – Return serialized search results for the given query.
        - `request_symbol_locations()` – Return symbol match snippets serialized with provenance.
        - `request_diff_bundle()` – Return a serialized diff bundle for the current working tree.
        - `attach_research_agent()` – Attach or detach the research agent used for external browsing.
        - `set_external_browsing_default()` – Configure the default browsing behaviour for subsequent runs.
        - `set_external_browsing()` – Manually override the browsing toggle for the current workflow.
        - `request_research()` – Return structured snippets for ``query`` and cache the evidence.
        - `get_research_evidence()` – Return cached research evidence scoped to ``audience`` if provided.
        - `refresh_mcp_tools()` – Refresh MCP tool metadata using live descriptors from ``registry``.

### `agents/repo_context.py`
Repository context agent providing code search, summaries, and git helpers.
#### Public classes
- **RepoSearchResult**
    Result returned from a repository search query.
    - Methods:
        - `to_dict()`
- **RepoSymbolResult**
    Match highlighting symbol locations inside a search snippet.
    - Methods:
        - `to_dict()`
- **FileSummary**
    Summary information for a file or module.
    - Methods:
        - `to_dict()`
- **DiffFileStat**
    Per-file statistics for a diff bundle.
    - Methods:
        - `to_dict()`
- **DiffBundle**
    Collection of staged or unstaged changes with provenance information.
    - Methods:
        - `to_dict()`
- **RepoContextAgent**
    Encapsulates repository-aware context retrieval and git utilities.
    - Methods:
        - `start_background_refresh()` – Start a periodic background refresh loop if not already running.
        - `stop_background_refresh()` – Stop the periodic refresh loop.
        - `refresh_index()` – Trigger a rebuild of the CodebaseRAG index.
        - `search()` – Execute a semantic search against the repository index.
        - `symbol_search()` – Locate symbol occurrences by combining semantic and lexical search.
        - `summarize_file()` – Return a simple textual summary for a file.
        - `summarize_ast()` – Produce a structure-oriented summary leveraging Python's AST when applicable.
        - `current_branch()` – Return the currently checked-out git branch.
        - `checkout()` – Checkout an existing branch or create a new one.
        - `stage()` – Stage changes for commit.
        - `diff_bundle()` – Collect patch text and file statistics for the current diff.
        - `focused_files()` – Convenience alias for manager requests.
        - `focused_diffs()` – Return a diff bundle suitable for manager consumption.
        - `detect_ci_systems()` – Return discovered CI configuration files grouped by provider.
        - `read_file()` – Read a repository file returning ``None`` when not found.
        - `update_file_if_changed()` – Write ``content`` only when it differs from the existing file.

### `agents/research.py`
High-level research helper built on top of :class:`internal.RAG.WebRAG`.
#### Public classes
- **ResearchSnippet**
    Structured excerpt extracted from a web source.
    - Methods:
        - `to_dict()`
- **ResearchResult**
    Container bundling the snippets for a single research query.
    - Methods:
        - `to_dict()`
        - `limit()`
- **ResearchAgent**
    Wrapper around :class:`WebRAG` providing caching and sanitisation.
    - Methods:
        - `clear_cache()` – Invalidate cached query and source lookups.
        - `search()` – Return cached or freshly gathered research snippets for ``query``.
- **VariedResearchAgent**
    Compose :class:`ResearchAgent` with profile-based search presets.
    - Methods:
        - `base_agent()`
        - `profiles()`
        - `modes()`
        - `search()`

### `agents/runner.py`
Execution helper responsible for spawning commands and tracking runs.
#### Public classes
- **RunReport**
    Structured report describing a single command execution.
    - Methods:
        - `to_dict()` – Serialize the report into a JSON-friendly payload.
        - `logs()` – Return a best-effort textual log for quick inspection.
- **RunnerAgent**
    High-level helper managing command execution and runtime lifecycle.
    - Methods:
        - `run_shell()` – Execute a shell command and capture a structured report.
        - `run_process()` – Spawn a background process using the unified process tool.
        - `get_reports()` – Return an immutable snapshot of the recorded run reports.
        - `clear_reports()` – Forget all stored run reports.
        - `install_dependencies()` – Ensure the provided dependencies are installed once per session.
        - `mark_dependency_installed()` – Record that a dependency is already satisfied.
        - `clear_dependency_cache()` – Reset the dependency installation cache.
        - `register_runtime()` – Register lifecycle hooks for a named runtime environment.
        - `prepare_runtime()` – Execute the setup hook for ``name`` and cache its state.
        - `teardown_runtime()` – Run the teardown hook for ``name`` if it has been prepared.
        - `reset_runtimes()` – Tear down all prepared runtimes.
        - `artifact_root()` – Directory used for storing generated artifacts.
        - `create_artifact_path()` – Return a path inside :attr:`artifact_root` for ``name``.

### `agents/security.py`
Security scanning orchestration utilities.
#### Public classes
- **SecurityCacheDirective**
    Hint describing cache paths that should be persisted between scans.
    - Methods:
        - `to_dict()`
        - `hint()`
- **SecurityScanFinding**
    Individual issue discovered by a security scan.
    - Methods:
        - `normalized_severity()`
        - `severity_rank()`
        - `to_dict()`
        - `describe()`
- **SecurityScanReport**
    Structured representation of a single toolchain run.
    - Methods:
        - `ok()`
        - `artifacts()`
        - `highest_severity()`
        - `severity_counts()`
        - `describe()`
        - `to_dict()`
- **SecurityScanResult**
    Aggregate outcome for a set of security scans.
    - Methods:
        - `highest_severity()`
        - `artifacts()`
        - `to_dict()`
- **SecurityToolchain**
    Configuration describing how to invoke and parse a scan tool.
    - Methods:
        - `with_overrides()`
- **SecurityAgent**
    Coordinate multiple security scanning toolchains via :class:`RunnerAgent`.
    - Methods:
        - `configure_toolchain()`
        - `get_toolchain()`
        - `run_scans()`
        - `format_result()`

### `agents/tester.py`
Testing critic agent responsible for lightweight gating checks.
#### Public classes
- **TestSuiteConfig**
    Configuration describing a single fast test or lint command.
    - Methods:
        - `from_mapping()`
- **TestSuiteResult**
    Result payload produced after executing a configured suite.
    - Methods:
        - `to_dict()`
- **CriticStatusEvent**
    Structured event emitted during critic execution.
    - Methods:
        - `to_dict()`
- **CriticAnalysis**
    Heuristic analysis of aggregated test outcomes.
- **TestCriticReport**
    Aggregate report returned by :class:`TestCriticAgent`.
    - Methods:
        - `is_blocking()`
        - `to_status_payload()`
        - `build_block_message()`
- **TestCriticAgent**
    Agent responsible for executing and analysing fast verification suites.
    - Methods:
        - `register_suite()`
        - `clear_suites()`
        - `set_status_callback()`
        - `has_status_callback()`
        - `last_report()`
        - `run_and_report()`

## `cli/`

### `cli/__init__.py`
CLI helpers shared between Auto-Coder entry points.

### `cli/overrides.py`
Shared helpers for CLI argument parsing and overrides construction.
#### Top-level functions
- `build_overrides()` – Build a nested overrides dictionary from parsed CLI arguments.
- `apply_common_flags()` – Attach shared CLI options used across Auto-Coder entry points.

## `internal/`

### `internal/DAG.py`
_No module-level docstring._
#### Public classes
- **DAGNode**
    A single node in a DAG.

    - name: unique identifier for the node
    - func: a callable that produces the node's value. It can return any value.
      The callable may declare optional parameters:
        - `context` or `ctx`: receives a DAGContext object
        - `inputs` or `_inputs`: receives a dict of upstream results keyed by node name
      In all cases, any `params` are also supplied as keyword args if supported.
    - deps: names of upstream nodes this node depends on
    - params: constant keyword arguments provided to the callable
    - value: if provided together with `is_constant=True`, the node acts as a constant
    - is_constant: when True, the node's value is the given `value` and func is ignored
    - retries: number of times to retry the callable on failure
    - retry_delay: seconds to wait between retries
    - timeout: optional timeout in seconds for this node. If exceeded, the attempt fails
    - metadata: free-form metadata for higher-level orchestration
- **NodeRunResult**
- **DAGContext**
    Execution-time context passed to node functions when requested.

    Attributes:
    - dag: the DAG instance
    - node: the DAGNode being executed
    - inputs: dict of upstream results {dep_name: value}
    - params: the node's params (shallow copy)
    - attempt: 1-based attempt counter for retries
- **DAG**
    A lightweight, dependency-aware task graph executor.

    Usage example:
        dag = DAG()
        dag.add_node("a", value=2, is_constant=True)
        dag.add_node("b", value=3, is_constant=True)

    …
    - Methods:
        - `add_node()`
        - `add_edge()` – Add a dependency edge `upstream -> downstream`.
        - `set_params()`
        - `set_constant()`
        - `nodes()`
        - `parents()`
        - `children()`
        - `leaves()`
        - `roots()`
        - `validate_acyclic()` – Raises ValueError if a cycle is found.
        - `topological_order()` – Return a topological ordering for the subgraph needed for `targets`.
        - `run()` – Execute the DAG and return results.
        - `to_dot()` – Return a Graphviz DOT representation of the (sub)graph.

### `internal/RAG.py`
_No module-level docstring._
#### Public classes
- **DocumentChunk**
- **CodebaseRAG**
    - Methods:
        - `build()`
        - `query()`
        - `rerank()`
- **DirectoryRAG**
    - Methods:
        - `build()`
        - `query()`
        - `rerank()`
- **SystemRAG**
    - Methods:
        - `build()`
        - `query()`
        - `rerank()`
- **WebRAG**
    - Methods:
        - `build_from_results()`
        - `search()`

### `internal/STT.py`
Speech-to-Text (STT) utilities implemented with Hugging Face Transformers.

Notes
- The LFM2-2.6B model documented in docs/LMF2-2.6B.md exposes a
  text-generation interface via `transformers`. It does not natively
  accept audio inputs. To provide full STT functionality, this module
  uses Transformers' ASR models (e.g., Whisper or Wav2Vec2) under the
  hood while keeping a small, clean API surface for later wiring.

…
#### Public classes
- **STTConfig**
    Configuration for the STT engine.

    - model_id: Hugging Face model id supporting ASR (e.g., Whisper/Wav2Vec2).
    - device: 'cpu', 'cuda', or device index like 'cuda:0'.
    - torch_dtype: Optional dtype string passed to model load (e.g., 'float16', 'bfloat16').
    - chunk_length_s: Optional chunk size for long audio in seconds (Whisper supports this).
    - batch_size: Pipeline batch size for batched inputs.
    - generate_kwargs: Extra kwargs forwarded to the underlying generate/decoding.
- **STT**
    High-level STT wrapper around a Transformers ASR pipeline.

    This intentionally does not wire into any external runtime. It simply
    offers programmatic transcription for an audio file path or a raw 
    waveform array.
    - Methods:
        - `transcribe()` – Transcribe an audio file path or waveform into text.

### `internal/TTS.py`
Text-to-Speech (TTS) utilities implemented with Hugging Face Transformers.

Notes
- The LFM2-2.6B model documented in docs/LMF2-2.6B.md is a text model.
  It does not synthesize audio. To provide TTS functionality we rely on
  Transformers' TTS-capable models (e.g., SpeechT5). This module keeps
  a small, reusable API surface ready for later wiring.

…
#### Public classes
- **TTSConfig**
    Configuration for the TTS engine.

    - model_id: TTS-capable HF model id (default: SpeechT5)
    - vocoder_id: Optional HiFi-GAN vocoder for SpeechT5
    - device: 'cpu', 'cuda', or device index like 'cuda:0'
    - torch_dtype: Optional dtype string
    - generate_kwargs: Extra kwargs forwarded to generation
    - sampling_rate: Target sampling rate for output audio
- **TTS**
    High-level TTS wrapper using Transformers SpeechT5 pipeline.

    Provides text-to-speech synthesis returning a waveform ndarray and
    sampling rate. Saving helpers are included for convenience.
    - Methods:
        - `synthesize()` – Synthesize speech audio from text.
        - `save_wav()` – Save a mono float32 waveform to a .wav file.

### `internal/agents.py`
Agent option dataclasses targeting the LM Studio Python SDK.

The helpers defined here are thin, typed containers that describe the most
common agent configuration knobs exposed by the LM Studio SDK (tested with
versions 0.3.3 and newer). Default values mirror the defaults shown in the LM
Studio application presets: ``temperature=0.7`` for balanced creativity,
``max_rounds=6`` to keep conversations bounded, and structured outputs are
opt-in with ``strict`` JSON schema validation enabled when requested.
#### Public classes
- **AgentConfigPayload**
    Typed dictionary describing configuration passed to the SDK.
- **StructuredResponseSettings**
    Options controlling LM Studio's structured output helpers.
    - Methods:
        - `as_response_format()` – Return the response_format payload accepted by LM Studio.
- **AgentOptions**
    Container describing common LM Studio agent configuration options.
    - Methods:
        - `build_payload()` – Assemble a dictionary compatible with LM Studio's chat helpers.

### `internal/memory_rag.py`
Hybrid memory retrieval utilities combining vector and keyword search.
#### Public classes
- **MemoryRetrievalConfig**
    Tunable parameters that control hybrid memory retrieval.
    - Methods:
        - `from_env()`
        - `to_dict()`
- **MemoryHit**
    Container for ranked memory results returned by :class:`MemoryRAG`.
    - Methods:
        - `content()`
        - `metadata()`
        - `provenance()`
        - `to_dict()`
- **MemoryRAG**
    Compose multiple memory stores into a hybrid retrieval pipeline.
    - Methods:
        - `query_memory()` – Return ranked memory chunks for ``text`` enriched with provenance.

### `internal/schemas.py`
Utilities for working with LM Studio structured output schemas.
#### Public classes
- **SchemaError**
    Raised when schema values cannot be normalized.
- **ModelSchema**
    Protocol for classes that provide a JSON schema definition.
    - Methods:
        - `model_json_schema()`
#### Top-level functions
- `normalize_schema()`
- `build_response_format()`
- `parse_structured_content()`

### `internal/structures.py`
Result containers and adapters for OpenAI-compatible responses.
#### Public classes
- **StructuredResponse**
    Representation of a prediction result.

    Attributes
    ----------
    raw_response:
        The original OpenAI-compatible response payload.
    content:
        The textual representation of the prediction.
    parsed:
        Structured data parsed from the response when a schema is requested.
    schema:
        Schema descriptor (if provided) used to guide parsing.
    structured:
        Flag indicating whether structured output was requested.
    - Methods:
        - `from_response()`
- **PredictionResultAdapter**
    Helper for assembling streaming prediction results.
    - Methods:
        - `add_chunk()` – Record a streamed chunk and accumulate its textual content.
        - `result()` – Return the final prediction result, parsing structured data on demand.

### `internal/web_playwright.py`
Utilities for interacting with the web via Playwright.

This module centralises the logic required to perform search queries and to
render web pages before extracting readable text.  The functionality is kept in
one place so that higher-level modules can depend on a thin abstraction without
having to manage browser lifecycles directly.
#### Public classes
- **PlaywrightWebClient**
    Small helper around the Playwright sync API.

    Parameters
    ----------
    browser : str
        The browser family to launch (``"chromium"`` by default).
    headless : bool
        Whether to launch the browser in headless mode.
    timeout_ms : int
        Default timeout applied to navigation and selector waits.
    user_agent : str | None
        Optional custom user-agent string for the created context.
    - Methods:
        - `is_available()` – Return True when Playwright can be imported and started.
        - `collect_search_results()` – Query DuckDuckGo and return structured search results.
        - `render_page_text()` – Return readable text for the provided URL.

## `internal/tools/`

### `internal/tools/file.py`
_No module-level docstring._
#### Top-level functions
- `create_file()`
- `read_file()`
- `write_file()`
- `append_to_file()`
- `patch_file()`
- `move_file()`
- `copy_file()`
- `delete_file()`
- `rename_file()`
- `open_file()`
- `open_folder()`
- `list_directory()`
- `make_directory()`
- `remove_directory()`
- `file_exists()`
- `path_info()`
- `search_in_files()`
- `file()` – Unified cross‑platform filesystem tool.

### `internal/tools/git.py`
_No module-level docstring._
#### Top-level functions
- `git_init()`
- `git_clone()`
- `git_add()`
- `git_commit()`
- `git_push()`
- `git_pull()`
- `git_status()`
- `git_log()`
- `git_branch()`
- `git_remote()`
- `git_checkout()`
- `git_checkout_new_branch()`
- `git_merge()`
- `git_fetch()`
- `git_rebase()`
- `git_tag()`
- `git_config_get()`
- `git_config_set()`
- `git()` – Unified Git tool that wraps common operations.

### `internal/tools/memory.py`
Tooling surface for interacting with the shared memory subsystem.
#### Top-level functions
- `memory_search_tool()` – Search agent memory for entries relevant to ``query``.
- `memory_add_tool()` – Add ``content`` into ``scope`` and return the stored record.
- `memory_update_tool()` – Update an existing memory record and return the new value.
- `memory_promote_tool()` – Promote a short-term memory into long-term storage.
- `memory_list_sessions_tool()` – Return the active memory sessions for ``scope``.

### `internal/tools/patch.py`
_No module-level docstring._
#### Top-level functions
- `patch()` – Patch and diff suite similar to git.

### `internal/tools/planner.py`
_No module-level docstring._
#### Top-level functions
- `planner()` – Planner tool with branching tasks and full plan management.

### `internal/tools/process.py`
_No module-level docstring._
#### Top-level functions
- `process()` – Unified process management tool.

### `internal/tools/shell.py`
_No module-level docstring._
#### Top-level functions
- `shell()` – Runs a command using the selected shell/runtime.

## `scripts/`

### `scripts/__init__.py`
Utility scripts packaged as importable modules.

### `scripts/demo_textui.py`
Generate a static screenshot of the Auto-Coder Text UI layout.
#### Public classes
- **TranscriptWidget**
- **PlanWidget**
    - Methods:
        - `load_sample()`
- **BudgetWidget**
    - Methods:
        - `load_sample()`
- **StatusWidget**
    - Methods:
        - `load_sample()`
- **PromptInput**
- **DemoTextUI**
    - Methods:
        - `compose()`
#### Top-level functions
- `main()`

### `scripts/generate_codebase_reference.py`
Generate a Markdown reference covering the core Auto-Coder modules.

This helper walks the repository tree, extracts module, class, and function
docstrings via the :mod:`ast` module, and renders a compact documentation page
summarising the public surface area.  Test modules are intentionally skipped to
keep the output focused on the production runtime.  Regenerate the reference
whenever you add, rename, or remove modules so contributors always have an
up-to-date map of the codebase.
#### Public classes
- **MethodInfo**
    Docstring metadata for a function or method.
- **ClassInfo**
    Docstring metadata for a class and its public methods.
- **ModuleInfo**
    Aggregated documentation for a module.
#### Top-level functions
- `iter_source_files()` – Return Python source files excluding test/docs/support directories.
- `trim_docstring()` – Normalise indentation and collapse docstrings to paragraphs.
- `collect_module_info()` – Parse a Python module and return docstring metadata.
- `render_module_section()` – Render a Markdown section for a single module.
- `build_documentation()` – Generate the full Markdown document.
- `main()` – Write the generated documentation to ``docs/autocoder/codebase-reference.md``.

### `scripts/setup_memory.py`
Command-line helper for preparing Redis and PostgreSQL memory backends.
#### Public classes
- **StepResult**
    Result emitted for each setup step.
    - Methods:
        - `format()`
#### Top-level functions
- `ensure_redis_ready()` – Ping Redis and provision RediSearch indexes when required.
- `ensure_postgres_ready()` – Run SQL migrations and validate pgvector availability.
- `main()`

